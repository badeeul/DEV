CREATE TABLE IF NOT EXISTS `dim_garage_location` (
  `garage_loc_surrogate_key` INT,
  `policy_num_bus_key` STRING,
  `garage_loc_bus_key` STRING,
  `garage_loc_number` STRING,
  `garage_loc_address1` STRING,
  `garage_loc_address2` STRING,
  `garage_loc_city` STRING,
  `garage_loc_state` STRING,
  `garage_loc_zipcode` STRING,
  `garage_loc_zipcode_plus_extension` STRING,
  `garage_loc_county` STRING,
  `garage_loc_latitude` DECIMAL(18, 6),
  `garage_loc_longitude` DECIMAL(18, 6),
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dimension table for garage locations";


CREATE TABLE IF NOT EXISTS `dim_trailer_type` (
  `trailer_type_surrogate_key` INT,
  `trailer_type_bus_key` STRING,
  `trailer_type_desc` STRING,
  `trailer_type_sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dimension table for trailer types";


CREATE TABLE IF NOT EXISTS `dim_vehicle` (
  `vehicle_surrogate_key` INT,
  `vehicle_type_surrogate_key` INT,
  `trailer_type_surrogate_key` INT,
  `policy_num_bus_key` STRING,
  `vehicle_bus_key` STRING,
  `vehicle_number` STRING,
  `vin` STRING,
  `vehicle_make` STRING,
  `vehicle_model` STRING,
  `vehicle_model_year` STRING,
  `vehicle_start_date` DATE,
  `vehicle_end_date` DATE,
  `vehicle_deleted_on_date` DATE,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dimension table for vehicles";


CREATE TABLE IF NOT EXISTS `dim_vehicle_radius_of_operation` (
  `vehicle_radius_of_operation_surrogate_key` INT,
  `vehicle_radius_of_operation_bus_key` STRING,
  `vehicle_radius_of_operation_desc` STRING,
  `vehicle_radius_of_operation_sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dimension table for vehicle radius of operation";


CREATE TABLE IF NOT EXISTS `dim_vehicle_type` (
  `vehicle_type_surrogate_key` INT,
  `vehicle_type_bus_key` STRING,
  `vehicle_type_desc` STRING,
  `vehicle_type_group` STRING,
  `power_unit_ind` STRING,
  `vehicle_type_sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dimension table for vehicle types";


CREATE TABLE IF NOT EXISTS `fact_vehicle` (
  `vehicle_surrogate_key` INT,
  `policy_surrogate_key` LONG,
  `garage_loc_surrogate_key` INT,
  `vehicle_radius_of_operation_surrogate_key` INT,
  `vehicle_start_date_surrogate_key` INT,
  `vehicle_end_date_surrogate_key` INT,
  `vehicle_deleted_on_date_surrogate_key` INT,
  `full_term_vehicle_flag` BOOLEAN,
  `mid_term_vehicle_flag` BOOLEAN,
  `voided_vehicle_flag` BOOLEAN,
  `deleted_vehicle_flag` BOOLEAN,
  `count_as_vehicle_flag` BOOLEAN,
  `count_as_power_unit_flag` BOOLEAN,
  `latest_vehicle_prem` DECIMAL(19, 4),
  `current_inforce_vehicle_prem` DECIMAL(19, 4),
  `vehicle_msrp` DECIMAL(19, 4),
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "fact table for vehicles";

