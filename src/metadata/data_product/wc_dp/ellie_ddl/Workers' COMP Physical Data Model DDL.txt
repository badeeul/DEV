CREATE TABLE IF NOT EXISTS `dim_wc_carrier_placement_points_rule` (
  `wc_carr_plcmnt_pts_rule_bus_key` STRING,
  `rating_type` STRING,
  `minimum_value` DECIMAL(8,4),
  `maximum_value` DECIMAL(8,4),
  `low_code` STRING,
  `high_code` STRING,
  `points_assigned` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "workers comp carrier placement points rules";

CREATE TABLE IF NOT EXISTS `dim_policy_extension_wc` (
  `policy_key` INT,
  `policy_num_bus_key` STRING,
  `is_multi_state_flag` BOOLEAN,
  `gov_hazard_group` STRING,
  `dnb_score` INT,
  `prior_wc_years_count` DECIMAL(8,4),
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "extension of dim_policy with workers comp specific attributes";

CREATE TABLE IF NOT EXISTS `dim_policy_extension_wc_carrier_placement` (
  `policy_key` INT,
  `policy_num_bus_key` STRING,
  `experience_mod` DECIMAL(8,4),
  `guardtech_hazard_group` STRING,
  `guardtech_hazard_group_points` INT,
  `guardtech_experience_mod` DECIMAL(8,4),
  `guardtech_experience_mod_points` INT,
  `guardtech_dnb_score` INT,
  `guardtech_dnb_score_points` INT,
  `guardtech_prior_wc_years_count` INT,
  `guardtech_prior_wc_years_count_points` INT,
  `guardtech_total_points` INT,
  `guardtech_enteredon` TIMESTAMP,
  `guardtech_carrier_select_group` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "extension of dim_policy with workers comp carrier placement attributes";

CREATE TABLE IF NOT EXISTS `segmentation_experience_mod` 
(
  `experience_mod_segment_key` INT,
  `experience_mod_segment_bus_key` STRING,
  `minimum_value` DECIMAL(8,4),
  `maximum_value` DECIMAL(8,4),
  `sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Defines experience modification factor ranges used for carrier placement scoring";

CREATE TABLE IF NOT EXISTS `segmentation_dnb_score` 
(
  `dnb_score_segment_key` INT,
  `dnb_score_segment_bus_key` STRING,
  `minimum_value` DECIMAL(8,4),
  `maximum_value` DECIMAL(8,4),
  `sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN  
) USING DELTA COMMENT "Defines DNB score ranges used for carrier placement segmentation and scoring";

CREATE TABLE IF NOT EXISTS `segmentation_prior_wc_years` 
(
  `prior_wc_years_segment_key` INT,
  `prior_wc_years_segment_bus_key` STRING,
  `minimum_value` DECIMAL(8,4),
  `maximum_value` DECIMAL(8,4),
  `sort_order` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN  
) USING DELTA COMMENT "Defines prior workers comp years ranges used for carrier placement scoring";

CREATE TABLE IF NOT EXISTS `wc_carrier_placement_snapshot` (
  `policy_key` INT,
  `policy_num_bus_key` STRING,
  `policy_sub_dt` DATE,
  `policy_effec_start_dt` DATE,
  `gov_state` STRING,
  `prod_src_desc` STRING,
  `gov_class_desc` STRING,
  `naics_industry` STRING,
  `superagency_nm` STRING,
  `agency_nm` STRING,
  `sub_point_of_origin_desc` STRING,
  `nb_submitted_count` INT,
  `nb_quoted_count` INT,
  `nb_issued_count` INT,
  `carrier_nm` STRING,
  `dna_hazard_group` STRING,
  `dna_hazard_group_points` INT,
  `guardtech_hazard_group` STRING,
  `guardtech_hazard_group_points` INT,
  `hazard_group_points_match_flag` STRING,
  `experience_mod_segment_key` INT,
  `dna_experience_mod` DECIMAL(8,4),
  `dna_experience_mod_points` INT,
  `guardtech_experience_mod` DECIMAL(8,4),
  `guardtech_experience_mod_points` INT,
  `experience_mod_points_match_flag` STRING,
  `dnb_score_segment_key` INT,
  `dna_dnb_score` INT,
  `dna_dnb_score_points` INT,
  `guardtech_dnb_score` INT,
  `guardtech_dnb_score_points` INT,
  `dnb_score_points_match_flag` STRING,
  `prior_wc_years_segment_key` INT,
  `dna_prior_wc_years_count` DECIMAL(8,4),
  `dna_prior_wc_years_count_points` INT,
  `guardtech_prior_wc_years_count` INT,
  `guardtech_prior_wc_years_count_points` INT,
  `prior_wc_years_points_match_flag` STRING,
  `all_category_points_match_flag` STRING,
  `dna_total_points` INT,
  `guardtech_total_points` INT,
  `guardtech_enteredon` TIMESTAMP,
  `guardtech_carrier_select_group` STRING
) USING DELTA COMMENT "Semantic snapshot combining policy, workers comp attributes, segmentation, and carrier placement scoring results";
