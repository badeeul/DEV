CREATE TABLE IF NOT EXISTS `dim_business_class` (
  `bus_class_key` INT,
  `bus_class_cd_bus_key` STRING,
  `bus_class_suffix_bus_key` STRING,
  `bus_class_src_id_bus_key` INT,
  `bus_class_desc` STRING,
  `bus_naics_cd` STRING,
  `bus_lob` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "This table list the business class associated with a policy. For now, WC and BOP would have business class";


CREATE TABLE IF NOT EXISTS `dim_cancellation_reasons` (
  `cancel_reason_key` INT,
  `cancel_reason_cd_bus_key` STRING,
  `cancel_reason_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "A code that represents the reason for policy cancellation. For example, Non-Payment of the deductible., Non-Payment of an Audit, etc.";


CREATE TABLE IF NOT EXISTS `dim_carrier` (
  `carrier_key` INT,
  `carrier_type` STRING,
  `carrier_nm` STRING,
  `carrier_cd_bus_key` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "An insurance company that provides coverage and assumes risk. A carrier can be classified as Admitted OR non-admitted.";


CREATE TABLE IF NOT EXISTS `dim_date` (
  `date_key` INT,
  `date` DATE,
  `day` INT,
  `day_suffix` STRING,
  `day_name` STRING,
  `day_of_week` INT,
  `day_of_week_in_month` INT,
  `day_of_year` INT,
  `is_weekend` BOOLEAN,
  `week` INT,
  `iso_week` INT,
  `first_of_week` DATE,
  `last_of_week` DATE,
  `week_of_month` INT,
  `month` INT,
  `month_name` STRING,
  `first_of_month` DATE,
  `last_of_month` DATE,
  `first_of_next_month` DATE,
  `last_of_next_month` DATE,
  `quarter` INT,
  `year_quarter` STRING,
  `first_of_quarter` DATE,
  `last_of_quarter` DATE,
  `year` INT,
  `iso_year` INT,
  `first_of_year` DATE,
  `last_of_year` DATE,
  `is_leap_year` BOOLEAN,
  `has_53_weeks` BOOLEAN,
  `has_iso_53_weeks` BOOLEAN,
  `mmyyyy` INT,
  `style101` DATE,
  `style103` DATE,
  `style112` DATE,
  `style120` DATE,
  `year_to_date` BOOLEAN,
  `prior_year_to_date` BOOLEAN,
  `prior_year` BOOLEAN,
  `prior_2_years` BOOLEAN,
  `trailing_12_months` BOOLEAN,
  `prior_month_ytd` BOOLEAN,
  `prior_month_pytd` BOOLEAN,
  `prior_month_ttm` BOOLEAN,
  `inforce_date_ytd` BOOLEAN,
  `inforce_date_pytd` BOOLEAN,
  `prior_month_py_ttm` BOOLEAN,
  `current_quarter` BOOLEAN,
  `prior_year_quarter` BOOLEAN,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Dim Date";


CREATE TABLE IF NOT EXISTS `dim_digital_decision_status` (
  `digital_decision_status_key` INT,
  `digital_decision_status_level` STRING,
  `digital_decision_status_cd_bus_key` STRING,
  `digital_decision_status_desc` STRING,
  `digital_decision_status_parent_cd_bus_key` STRING,
  `digital_decision_status_parent_key` INT,
  `dd_l0_level` STRING,
  `dd_l1_level` STRING,
  `dd_l2_level` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "list of digital decision statuses, these apply only to new business submissions";


CREATE TABLE IF NOT EXISTS `dim_dist_chnl` (
  `dist_chnl_key` INT,
  `agcy_cd_bus_key` STRING,
  `agcy_desc` STRING,
  `super_agcy_cd` STRING,
  `super_agcy_desc` STRING,
  `terr_mngr_emp_key` INT,
  `prod_rel_adv_emp_key` INT,
  `agcy_status` STRING,
  `agcy_closed_date` DATE,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Distribution channel underwrites policies, generate business and works with Carrier to issue policies. Delegated Authority and In-house underwriting are two types of distribution channels. While Managing General Agents ( MGA ) are delegated authority, Retail Agency and Wholesale Agency are In-house underwriters.";


CREATE TABLE IF NOT EXISTS `dim_employee` (
  `emp_key` INT,
  `emp_cd_bus_key` STRING,
  `emp_nm` STRING,
  `job_title` STRING,
  `user_id` STRING,
  `term_dt` DATE,
  `active` BOOLEAN,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "this table represents many team members including Underwriters, Territory Manager, Producer Relations Advisor, Created by,  Updated by.";


CREATE TABLE IF NOT EXISTS `dim_insured` (
  `insd_key` INT,
  `policy_num_bus_key` STRING,
  `mail_add1` STRING,
  `mail_add2` STRING,
  `mail_city` STRING,
  `mail_county` STRING,
  `mail_state` STRING,
  `mail_zip` STRING,
  `insd_nm` STRING,
  `legal_ent_type_key` INT,
  `bus_start_year` STRING,
  `desc_of_ops` STRING,
  `crmid` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "this table stores the information about policy holders";


CREATE TABLE IF NOT EXISTS `dim_insured_legal_entity_type` (
  `insd_leg_ent_type_key` INT,
  `insd_leg_ent_type_cd_bus_key` STRING,
  `insd_leg_ent_type_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA;


CREATE TABLE IF NOT EXISTS `dim_lob` (
  `lob_key` INT,
  `lob_cd_bus_key` STRING,
  `lob_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "A category of insurance that groups similar types of policies together based on the nature of risk covered. For example, Homeowners Insurance is a line of business for residential property coverage.";


CREATE TABLE IF NOT EXISTS `dim_lob_product` (
  `lob_product_key` INT,
  `lob_product_id_bus_key` INT,
  `lob_product_desc` STRING,
  `lob_product_keyidentifier` STRING,
  `lob_product_lob` STRING,
  `lob_product_effectivelob` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "A category of line of business specific product designations. For example, Type of Professional Liability policy - Lawyers, Architects and Engineers, Accountants, Miscellaneous.";


CREATE TABLE IF NOT EXISTS `dim_mkt_type` (
  `mkt_type_key` INT,
  `mkt_type_cd_bus_key` STRING,
  `mkt_type_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Market Type is the general marketplace structure the business comes from. For example, if could come through the voluntary market where you are free to go any company or the residual where a risk is auto place.";


CREATE TABLE IF NOT EXISTS `dim_naics` (
  `naics_key` INT,
  `naics_bus_key` STRING,
  `naics_desc` STRING,
  `naics_industry` STRING,
  `naics_sub_industry` STRING,
  `naics_bus_type` STRING,
  `naics_two_digit` STRING,
  `naics_three_digit` STRING,
  `naics_four_digit` STRING,
  `naics_five_digit` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "The North American Industry Classification System (NAICS) is the standard used by Federal statistical agencies in classifying business establishments for the purpose of collecting, analyzing, and publishing statistical data related to the U.S. business economy. NAICS entity at Guard also include consists  Industry, Sub-industry and Business-type.";


CREATE TABLE IF NOT EXISTS `dim_policy` (
  `policy_key` INT,
  `policy_num_bus_key` STRING,
  `policy_bus_type` STRING,
  `gov_state` STRING,
  `policy_effec_start_dt` DATE,
  `policy_effec_end_dt` DATE,
  `policy_coverage_start_dt` DATE,
  `policy_coverage_end_dt` DATE,
  `policy_cancel_dt` DATE,
  `policy_cancel_trans_dt` DATE,
  `policy_audit_dt` DATE,
  `policy_sub_dt` DATE,
  `policy_issue_dt` DATE,
  `offer_rn` BOOLEAN,
  `previous_policy_num_bus_key` STRING,
  `next_policy_num_bus_key` STRING,
  `chain_id` STRING,
  `first_dwnpymnt_recd_dt` DATE,
  `rewritten_reissued_ind` STRING,
  `rewrite_reissue_ind` STRING,
  `is_expiring_flag` BOOLEAN,
  `is_renewable_flag` STRING,
  `is_prev_policy_ubetag_flag` BOOLEAN,
  `is_cloned_flag` BOOLEAN,
  `src_policy_status_descrip` STRING,
  `latest_policy_status_key` INT,
  `digital_decision_status_key` INT,
  `autouw_status_desc` STRING,
  `sub_point_of_origin_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "A contract between an insurance company and the policyholder/insured that outlines the coverage provided, premiums, deductibles, and terms.";


CREATE TABLE IF NOT EXISTS `dim_policy_status` (
  `policy_status_key` INT,
  `policy_status_level` STRING,
  `policy_status_cd_bus_key` STRING,
  `policy_status_desc` STRING,
  `policy_status_parent_cd_bus_key` STRING,
  `policy_status_parent_key` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN,
  `l0_level` STRING,
  `l1_level` STRING,
  `l2_level` STRING,
  `l3_level` STRING,
  `l4_level` STRING,
  `l5_level` STRING
) USING DELTA COMMENT "list of policy statuses, it includes policy life cycle messages";


CREATE TABLE IF NOT EXISTS `dim_policy_trans_reason` (
  `policy_trans_reason_key` INT,
  `policy_trans_reason_cd_bus_key` STRING,
  `policy_trans_reason_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA;


CREATE TABLE IF NOT EXISTS `dim_policy_trans_subtype` (
  `policy_trans_subtype_key` INT,
  `policy_trans_subtype_bus_key` INT,
  `policy_trans_cd` STRING,
  `policy_trans_subtype_desc` STRING,
  `policy_trans_subtype_desc_short` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA;


CREATE TABLE IF NOT EXISTS `dim_policy_trans_type` (
  `policy_trans_type_key` INT,
  `policy_trans_type_cd_bus_key` STRING,
  `policy_trans_type_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Policy Transaction Types";


CREATE TABLE IF NOT EXISTS `dim_prod_src` (
  `prod_src_key` INT,
  `prod_src_cd_bus_key` STRING,
  `prod_src_desc` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Production source is who is producing that business (i.e. generally an agency type or distribution type - does it come from a wholesaler vs a retailer which is a completely different type of agency structure and is generally handled differently by an insurance carrier.)";


CREATE TABLE IF NOT EXISTS `fact_policy` (
  `policy_key` INT,
  `policy_effec_start_dt_key` INT,
  `policy_effec_end_dt_key` INT,
  `policy_cancel_dt_key` INT,
  `policy_sub_dt_key` INT,
  `policy_coverage_start_dt_key` INT,
  `policy_coverage_end_dt_key` INT,
  `policy_issue_dt_key` INT,
  `policy_audit_dt_key` INT,
  `lob_key` INT,
  `dist_chnl_key` INT,
  `naics_key` INT,
  `dec_uw_emp_key` INT,
  `mkt_type_key` INT,
  `prod_src_key` INT,
  `carrier_key` INT,
  `dec_prem` DECIMAL(19, 4),
  `dw_prem` DECIMAL(19, 4),
  `entered_by_emp_key` INT,
  `insd_key` INT,
  `gov_bus_class_key` INT,
  `lob_product_key` INT,
  `nb_submitted_prem` DECIMAL(19, 4),
  `nb_submitted_count` TINYINT,
  `nb_quoted_prem` DECIMAL(19, 4),
  `nb_quoted_count` TINYINT,
  `nb_issued_prem` DECIMAL(19, 4),
  `nb_issued_count` TINYINT,
  `rn_quoted_prem` DECIMAL(19, 4),
  `rn_quoted_count` TINYINT,
  `rn_issued_prem` DECIMAL(19, 4),
  `rn_issued_count` TINYINT,
  `rn_issued_dwnpymnt_recd_prem` DECIMAL(19, 4),
  `rn_issued_dwnpymnt_recd_count` TINYINT,
  `expiring_prem` DECIMAL(19, 4),
  `expiring_count` TINYINT,
  `expiring_renewable_prem` DECIMAL(19, 4),
  `expiring_renewable_count` TINYINT,
  `next_policy_quoted_prem` DECIMAL(19, 4),
  `next_policy_quoted_count` TINYINT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "Policy dimension";


CREATE TABLE IF NOT EXISTS `fact_policy_transaction` (
  `policy_key` INT,
  `policy_effec_start_dt_key` INT,
  `policy_effec_end_dt_key` INT,
  `policy_coverage_start_dt_key` INT,
  `policy_coverage_end_dt_key` INT,
  `lob_key` INT,
  `dist_chnl_key` INT,
  `insd_key` INT,
  `naics_key` INT,
  `dec_uw_emp_key` INT,
  `mkt_type_key` INT,
  `prod_src_key` INT,
  `carrier_key` INT,
  `policy_trans_type_key` INT,
  `policy_trans_effec_dt_key` INT,
  `policy_trans_dt_key` INT,
  `policy_trans_seq_num` INT,
  `src_trans_id_type` INT,
  `src_trans_id` INT,
  `prem_amt` DECIMAL(19, 4),
  `policy_trans_written_on_dt_key` INT,
  `fsa_amt` DECIMAL(19, 4),
  `paa_amt` DECIMAL(19, 4),
  `sf3_amt` DECIMAL(19, 4),
  `sf4_amt` DECIMAL(19, 4),
  `sf5_amt` DECIMAL(19, 4),
  `sf6_amt` DECIMAL(19, 4),
  `sf7_amt` DECIMAL(19, 4),
  `comm_amt` DECIMAL(19, 4),
  `non_comm_prem_amt` DECIMAL(19, 4),
  `gov_bus_class_key` INT,
  `lob_product_key` INT,
  `policy_trans_state` STRING,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA COMMENT "events / transactions as tracked in the source system for a policy";


CREATE TABLE IF NOT EXISTS `fact_policy_transaction_detail` (
  `policy_key` INT,
  `policy_effec_start_dt_key` INT,
  `policy_effec_end_dt_key` INT,
  `policy_coverage_start_dt_key` INT,
  `policy_coverage_end_dt_key` INT,
  `lob_key` INT,
  `dist_chnl_key` INT,
  `insd_key` INT,
  `naics_key` INT,
  `dec_uw_emp_key` INT,
  `mkt_type_key` INT,
  `prod_src_key` INT,
  `carrier_key` INT,
  `policy_trans_type_key` INT,
  `policy_trans_effec_dt_key` INT,
  `policy_trans_dt_key` INT,
  `policy_trans_seq_num` INT,
  `src_trans_detail_id` INT,
  `policy_trans_subtype_key` INT,
  `extra_info` STRING,
  `additional_info` STRING,
  `gov_bus_class_key` INT,
  `lob_product_key` INT,
  `cancel_reason_key` INT,
  `dl_eltid` STRING,
  `dl_runid` STRING,
  `dl_row_insert_agent` STRING,
  `dl_row_hash` STRING,
  `dl_is_current_flag` BOOLEAN,
  `dl_row_expiration_date` DATE,
  `dl_row_effective_date` DATE,
  `dl_row_update_timestamp` TIMESTAMP,
  `dl_row_insert_timestamp` TIMESTAMP,
  `dl_is_deleted_flag` BOOLEAN
) USING DELTA;